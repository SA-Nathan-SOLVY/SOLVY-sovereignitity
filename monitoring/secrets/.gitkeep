# This directory holds runtime secrets.
# DO NOT commit files in this directory to git.
#
# Files expected here:
#   grafana_admin_password.txt  — Grafana admin password
#
# The .gitkeep file ensures the directory exists in the repo
# but the actual secrets are generated at deploy time.
