version: '3.8'

services:
  solvy-platform:
    build: .
    container_name: solvy-platform
    ports:
      - "8080:80"
    restart: unless-stopped
    
  # Optional: Add a reverse proxy for production
  # nginx-proxy:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx-proxy.conf:/etc/nginx/conf.d/default.conf:ro
  #   depends_on:
  #     - solvy-platform
  #   restart: unless-stopped
