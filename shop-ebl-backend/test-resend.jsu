import { Resend } from 'resend';

// Use the environment variable or paste your NEW key here temporarily
const apiKey = process.env.RESEND_API_KEY || 're_6ezJWizK_7hXcw4FSHM8hpDxHv71UQbNY';
const resend = new Resend(apiKey);

async function sendTest() {
  try {
    console.log('Attempting to send email...');
    const data = await resend.emails.send({
      from: 'SOLVY <onboarding@resend.dev>',
      to: ['s.a.nathanllc@gmail.com'], // CHANGE THIS!
      subject: 'SOLVY VPS Email Test',
      html: '<p>If you can read this, Resend is working on your VPS!</p>',
    });
    console.log('✅ Email sent successfully:', data);
  } catch (error) {
    console.error('❌ Error sending email:', error);
  }
}

sendTest();
